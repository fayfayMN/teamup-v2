# TeamUp needs NO secrets.
#
# It is key-less by design: matching is pure Python and data is stored in a local
# JSON file (teamup_state.json). There is nothing to configure here, and the
# Streamlit Cloud "Secrets" box should be left EMPTY.
